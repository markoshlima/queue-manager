import json
import boto3

sqs = boto3.client('sqs')

# send message to a queue to SQS
def send_to_sqs(queue, message):
	msg = sqs.send_message(QueueUrl = queue, MessageBody = json.dumps(message))
	return msg
	